# YasserOS minimal build profile
# Smallest possible image — no SSH, no extras
# source build-profiles/minimal.conf && ./build-docker.sh

source "$(dirname "$0")/default.conf"

IMG_NAME="yasseros-minimal"
TARGET_HOSTNAME="yasseros"
ENABLE_SSH=0
GPU_MEM=64
DEPLOY_ZIP=1
